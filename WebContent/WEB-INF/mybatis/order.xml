<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="order">
	<select id="selectOrderList" resultType="com.kosateam2.chicken.dto.ChickenMemberAndOrder">
		select oid, o.mid, mname, MPHONENUMBER, did, price, lat, lng, datetime, ostatus
		from orders o, members m
		where o.mid=m.mid
		and ostatus in (0,1)
	</select>


</mapper>